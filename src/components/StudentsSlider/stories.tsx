import { Story, Meta } from '@storybook/react';
import StudentsSlider from '.';

export default {
  title: 'StudentsSlider',
  component: StudentsSlider
} as Meta;

export const Default: Story = () => <StudentsSlider />;
