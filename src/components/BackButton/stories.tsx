import { Story, Meta } from '@storybook/react';
import BackButton from '.';

export default {
  title: 'BackButton',
  component: BackButton
} as Meta;

export const Default: Story = () => <BackButton />;
